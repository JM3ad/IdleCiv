import * as ko from 'knockout';
import * as fabric from 'fabric';
import {Producer} from '../purchasable/producer';

export class Painter{
    /*paint: (producers: Producer[]) => void;
    canvas: fabric.fabric.Canvas;
    
    constructor(){
        const currentCanvas = $('#canvas');
        this.canvas = new fabric.fabric.Canvas('canvas');
        // this.canvas.setDimensions({width: currentCanvas.width() ?? 0, height:currentCanvas.height()});
        this.paint = (producers: Producer[]) => {
            this.canvas.clear();

            producers.forEach(producer => {
                
                var rect = new fabric.fabric.Rect({
                    left: producer.image().locationX(),
                    top: producer.image().locationY(),
                    fill: 'red',
                    width: 10,
                    height: 10
                });
                
                this.canvas.add(rect);
            });
            
            this.canvas.renderAll();
        }
        
    }

*/
}